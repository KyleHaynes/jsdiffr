% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/html.R
\name{diff_html_select}
\alias{diff_html_select}
\title{Show a vector diff as a row-selectable HTML table}
\usage{
diff_html_select(
  vec_1,
  vec_2,
  method = diff_chars,
  ignore_words = NULL,
  view = TRUE,
  viewer = NULL,
  viewer_row_limit = 300
)
}
\arguments{
\item{vec_1, vec_2}{Character vectors of equal length to compare.}

\item{method}{Diff function applied per element pair. Defaults to \link{diff_chars}.}

\item{ignore_words}{Optional character vector of words to suppress from diff
highlighting. Changes consisting entirely of these words (case-insensitive)
are rendered as plain context rather than additions/deletions. Most useful
with \code{method = diff_words}.}

\item{view}{If \code{TRUE}, write to a temp file and display it (see \code{viewer}).}

\item{viewer}{Function used to display the temp file when \code{view = TRUE}.
Defaults to \code{NULL}, which auto-detects an in-editor viewer at call time:
\code{getOption("viewer")} (set by RStudio and by the VS Code / Positron R
extensions when their session watcher is attached), then
\code{rstudioapi::viewer} if the 'rstudioapi' package reports one is available.
Falls back to \code{\link[utils:browseURL]{utils::browseURL()}} (with a one-line \code{\link[=message]{message()}} explaining
why) when neither is found. Pass \code{utils::browseURL} explicitly to force
the system browser (or skip the row-count check below).}

\item{viewer_row_limit}{When \code{viewer} is left at its default \code{NULL}, diffs
with more than this many rows skip the in-editor viewer (RStudio/VS
Code's webview panel is slow to open for large tables) and open in the
system browser instead, which handles large static HTML files fine. Has
no effect if \code{viewer} is supplied explicitly. Set to \code{Inf} to always use
the in-editor viewer.}
}
\value{
The HTML string, invisibly when \code{view = TRUE}.
}
\description{
Like \code{\link[=diff_html]{diff_html()}}, but adds a checkbox beside the Left column and another
beside the Right column so individual rows can be marked. A floating
overlay tracks the checked rows live and displays them as an R vector
literal (e.g. \code{c(2, 22, 33)}) for each side, with a Copy button, so the
selection can be pasted straight into subsetting code such as
\code{vec_1[c(2, 22, 33)]}. Row numbers are 1-based and match positions in
\code{vec_1}/\code{vec_2}. A checkbox in each header column selects/clears all rows
on that side at once.
}
\examples{
diff_html_select(c("a", "b", "c"), c("a", "B", "c"), view = FALSE)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diff-text.R
\name{diff_lines}
\alias{diff_lines}
\title{Diff two strings line by line}
\usage{
diff_lines(
  old_str,
  new_str,
  ignore_whitespace = FALSE,
  ignore_newline_at_eof = FALSE,
  newline_is_token = FALSE,
  strip_trailing_cr = FALSE,
  ignore_case = FALSE,
  max_edit_length = NULL,
  timeout = NULL,
  one_change_per_token = FALSE
)
}
\arguments{
\item{old_str, new_str}{Length-1 character strings to compare.}

\item{ignore_whitespace}{If \code{TRUE}, leading/trailing whitespace is ignored
when comparing lines.}

\item{ignore_newline_at_eof}{If \code{TRUE}, a missing trailing newline at the end
of the file does not by itself cause the last line to differ.}

\item{newline_is_token}{If \code{TRUE}, newlines are emitted as their own tokens
instead of being attached to the preceding line.}

\item{strip_trailing_cr}{If \code{TRUE}, \verb{\\r\\n} is normalised to \verb{\\n} before
diffing.}

\item{ignore_case}{If \code{TRUE}, characters differing only in case are treated
as equal.}

\item{max_edit_length}{If set, abort and return \code{NULL} once the edit distance
exceeds this value.}

\item{timeout}{If set (milliseconds), abort and return \code{NULL} if the diff
takes longer than this.}

\item{one_change_per_token}{If \code{TRUE}, emit one change object per token
rather than coalescing runs of like changes.}
}
\value{
A \link{jsdiff_changes} object.
}
\description{
Diff two strings line by line
}
\examples{
diff_lines("line one\nline two\n", "line one\nline 2\n")
}

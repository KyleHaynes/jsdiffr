% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diff-json.R
\name{diff_json}
\alias{diff_json}
\title{Diff two JSON-serialisable values}
\usage{
diff_json(
  old_val,
  new_val,
  undefined_replacement = NULL,
  strip_trailing_cr = FALSE,
  max_edit_length = NULL,
  timeout = NULL,
  one_change_per_token = FALSE
)
}
\arguments{
\item{old_val, new_val}{R objects to serialise and compare, or length-1
character strings of pre-serialised JSON.}

\item{undefined_replacement}{Value substituted for \code{NULL}/\code{NA} leaves during
serialisation (jsdiff's \code{undefinedReplacement}). Defaults to JSON \code{null}.}

\item{strip_trailing_cr}{If \code{TRUE}, normalise \verb{\\r\\n} to \verb{\\n} before diffing.}

\item{max_edit_length}{If set, abort and return \code{NULL} once the edit distance
exceeds this value.}

\item{timeout}{If set (milliseconds), abort and return \code{NULL} if the diff
takes longer than this.}

\item{one_change_per_token}{If \code{TRUE}, emit one change object per token
rather than coalescing runs of like changes.}
}
\value{
A \link{jsdiff_changes} object.
}
\description{
Each input is serialised to pretty-printed JSON with object keys sorted, then
compared line by line. A character-scalar input is treated as pre-serialised
JSON and used as-is. Lines that differ only by a trailing comma are
considered equal (matching jsdiff).
}
\examples{
diff_json(list(a = 1, b = 2), list(a = 1, b = 3))
}

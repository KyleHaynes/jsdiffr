% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diff-arrays.R
\name{diff_arrays}
\alias{diff_arrays}
\title{Diff two sequences (arrays)}
\usage{
diff_arrays(
  old_arr,
  new_arr,
  comparator = NULL,
  max_edit_length = NULL,
  timeout = NULL,
  one_change_per_token = FALSE
)
}
\arguments{
\item{old_arr, new_arr}{Atomic vectors or lists to compare element-wise.}

\item{comparator}{Optional function \verb{function(left, right)} returning \code{TRUE}
when two elements should be considered equal. Must define an equivalence
relation. Defaults to strict equality.}

\item{max_edit_length}{If set, abort and return \code{NULL} once the edit distance
exceeds this value.}

\item{timeout}{If set (milliseconds), abort and return \code{NULL} if the diff
takes longer than this.}

\item{one_change_per_token}{If \code{TRUE}, emit one change object per token
rather than coalescing runs of like changes.}
}
\value{
A \link{jsdiff_changes} object whose \code{value} column is a list of
sub-sequences.
}
\description{
Diff two sequences (arrays)
}
\examples{
d <- diff_arrays(c("a", "b", "c"), c("a", "c", "d"))
d$value
}

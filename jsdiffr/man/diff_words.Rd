% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diff-text.R
\name{diff_words}
\alias{diff_words}
\title{Diff two strings word by word}
\usage{
diff_words(
  old_str,
  new_str,
  ignore_case = FALSE,
  ignore_whitespace = NULL,
  max_edit_length = NULL,
  timeout = NULL,
  one_change_per_token = FALSE
)
}
\arguments{
\item{old_str, new_str}{Length-1 character strings to compare.}

\item{ignore_case}{If \code{TRUE}, characters differing only in case are treated
as equal.}

\item{ignore_whitespace}{Backwards-compatibility shim from jsdiff: if
explicitly \code{FALSE}, behaves like \code{\link[=diff_words_with_space]{diff_words_with_space()}}.}

\item{max_edit_length}{If set, abort and return \code{NULL} once the edit distance
exceeds this value.}

\item{timeout}{If set (milliseconds), abort and return \code{NULL} if the diff
takes longer than this.}

\item{one_change_per_token}{If \code{TRUE}, emit one change object per token
rather than coalescing runs of like changes.}
}
\value{
A \link{jsdiff_changes} object.
}
\description{
Whitespace is preserved in the output but ignored when deciding whether two
words match (use \code{\link[=diff_words_with_space]{diff_words_with_space()}} to treat whitespace as
significant).
}
\examples{
diff_words("The quick brown fox", "The slow brown fox")
}
